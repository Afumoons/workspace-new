# 2026-03-06

- WhatsApp mapping:
  - +628170090022 ? Afu (user)
  - +6287744554544 ? younger sibling (adik)
  - +6285857292260 ? partner (pacar)
- Names:
  - Younger sibling (adik) at +6287744554544 ? Budi
  - Partner (pacar) at +6285857292260 ? Bunga
- Afu told me about their partner: name **Bunga**, likes food and likes **EXO**.
- Started working on **Chatties**, a real-time messaging project in Laravel.
- Project path: `C:\laragon\www\chatties`.
- Features observed: conversations, threaded messages, reactions, mentions, presence/typing indicators via Laravel Echo/Reverb.
- Today we:
  - Added `App\Http\Resources\MessageResource` to standardize message JSON (including replies + reactions).
  - Added `ChatController::messages(Conversation $conversation)` to expose messages as an API endpoint.
  - Registered route `GET chat/{conversation}/messages` named `chat.messages`.
- Next likely steps: refactor frontend `initChat()` to use this API endpoint and move chat JS into a dedicated module file.