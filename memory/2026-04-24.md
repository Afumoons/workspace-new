# 2026-04-24

- Daily governance audit run for `autonomous_trading_ai` completed around 10:10 WIB.
- Repo health snapshot: only visible git diff was `strategies/pool_state.json`; no maintenance writes applied during audit.
- Current live account snapshot from repo state: equity start 8345.29, equity current 8542.97, daily PnL +197.68 (+2.37%), 3 trades today, no day lock.
- Pool snapshot: 117 strategies total with status mix `active=34`, `exploratory=7`, `candidate=21`, `disabled=55`; symbol mix `BTCUSDm=97`, `XAUUSDm=20`.
- Live manifest/index were internally consistent and freshly generated at 2026-04-24T03:12:15Z: manifest `entry_count=32` with mix `active=30`, `exploratory=2`, evenly split `BTCUSDm=16` / `XAUUSDm=16`.
- Strategy-index family mix: overall `ma_trend=77`, `rsi_range=24`, `pullback_trend=15`, `generic=1`; active mix `ma_trend=22`, `rsi_range=10`, `generic=1`, `pullback_trend=1`.
- Drift signal remains the main warning: non-zero live-attributed active strategies summed about `-2263.67` over 86 trades despite strong backtest scores. Worst active examples: `core15_BTCUSDm_M15_e511` (-1148.90 over 4 live trades, backtest return +21.76%), `core15_XAUUSDm_M15_627c` (-412.28 over 9 trades, backtest return +28.65%), `core15_BTCUSDm_M15_f4f5` (-300.59 over 11 trades), `core15_BTCUSDm_M15_1a34` (-251.22 over 26 trades), `core15_BTCUSDm_M15_df0a` (-114.96 over 13 trades).
- Family-level live attribution on current-pool members with non-zero trades: `rsi_range` about `-2506.19` over 40 trades, `ma_trend` about `-1418.05` over 152 trades, `pullback_trend` +3.64 over 1 trade.
- `execution/strategy_live_stats.json` still contains significant attribution residue: 53 tracked buckets, but 39 strategy IDs no longer exist in current pool; `execution/unmatched_closed_deals.json` is now empty, so current issue is stale historical buckets rather than active unmatched backlog.
- Logs show system activity continuing this morning (research-memory queries and fresh file writes), so not stalled, but there was a runtime error at `2026-04-24 00:54:07` where `job_live_monitor` hit `MT5 account_info() returned None`; nearby logs also emitted live-decay warnings for `core15_BTCUSDm_M15_df0a`, `core15_BTCUSDm_M15_f4f5`, `core15_BTCUSDm_M15_1a34`, and `core15_XAUUSDm_M15_8fd0`.
- Storage/cardinality note: `strategies/generated/` contains 4044 JSON files while only 117 are in current pool; safe to leave alone in daily cron, but any cleanup would need explicit approval because generated strategy deletion is destructive by current rules.
- No safe maintenance was performed because manifest/index were already fresh and the most obvious cleanup lever is destructive historical pruning, which daily cron should not do without Afu approval.
- 10:18 WIB: Prepared morning market brief for Afu. Public snapshot used: DXY around 98.8 and first weekly gain in 3 weeks, US10Y around 4.33 and firm for 5 straight sessions, gold around 4677 and under pressure below 4700, WTI around 96-97 after a sharp weekly surge, US equities mixed after Thursday dip but futures steadier on Intel-led earnings support, BTC hovering around 78k-79k with institutional flow backdrop positive but major resistance near 79k-80k. Core read: macro is risk-fragile rather than clean risk-on because oil shock + firmer yields support USD and cap duration/gold upside; BTC and equities still have selective upside if headlines calm, but headline risk from Hormuz/Iran remains the main intraday regime switch.
- autonomous_trading_ai: created `scripts/repair_legacy_pool_metadata.py` to backfill missing `strategy_explain.meta` fields in `strategies/pool_state.json`; correct invocation is `python -m autonomous_trading_ai.scripts.repair_legacy_pool_metadata` because direct script execution caused `ModuleNotFoundError` for package imports.
- Running the metadata repair refreshed pool artifacts safely: `Pool loaded/saved: 117 strategies`, `live_manifest.json entries=32`, `strategy_index.json entries=117`, and `Repaired 88 legacy records`.
- Afu explicitly asked to persist both the repair script and refreshed `strategies/pool_state.json` to git.
- Created `scripts/reconstruct_trade_regime_session_journal.py` to reconstruct trade-level journal from `execution/trades.log`, `execution/ticket_strategy_map.json`, live stats, and feature parquet files; output path is `execution/strategy_trade_journal.json`.
- First reconstruction run succeeded in writing the journal and reconstructing 99 trade rows across 53 live-stat strategy buckets, but entry/exit regime-session context remained empty because symbol names in logs looked like `XAUUSD`/`BTCUSD` while available feature parquet files used variants like `XAUUSDm`/`BTCUSDm`; next fix is symbol canonicalization/fallback mapping.
# 2026-04-24

- Daily governance audit run for `autonomous_trading_ai` completed around 10:10 WIB.
- Repo health snapshot: only visible git diff was `strategies/pool_state.json`; no maintenance writes applied during audit.
- Current live account snapshot from repo state: equity start 8345.29, equity current 8542.97, daily PnL +197.68 (+2.37%), 3 trades today, no day lock.
- Pool snapshot: 117 strategies total with status mix `active=34`, `exploratory=7`, `candidate=21`, `disabled=55`; symbol mix `BTCUSDm=97`, `XAUUSDm=20`.
- Live manifest/index were internally consistent and freshly generated at 2026-04-24T03:12:15Z: manifest `entry_count=32` with mix `active=30`, `exploratory=2`, evenly split `BTCUSDm=16` / `XAUUSDm=16`.
- Strategy-index family mix: overall `ma_trend=77`, `rsi_range=24`, `pullback_trend=15`, `generic=1`; active mix `ma_trend=22`, `rsi_range=10`, `generic=1`, `pullback_trend=1`.
- Drift signal remains the main warning: non-zero live-attributed active strategies summed about `-2263.67` over 86 trades despite strong backtest scores. Worst active examples: `core15_BTCUSDm_M15_e511` (-1148.90 over 4 live trades, backtest return +21.76%), `core15_XAUUSDm_M15_627c` (-412.28 over 9 trades, backtest return +28.65%), `core15_BTCUSDm_M15_f4f5` (-300.59 over 11 trades), `core15_BTCUSDm_M15_1a34` (-251.22 over 26 trades), `core15_BTCUSDm_M15_df0a` (-114.96 over 13 trades).
- Family-level live attribution on current-pool members with non-zero trades: `rsi_range` about `-2506.19` over 40 trades, `ma_trend` about `-1418.05` over 152 trades, `pullback_trend` +3.64 over 1 trade.
- `execution/strategy_live_stats.json` still contains significant attribution residue: 53 tracked buckets, but 39 strategy IDs no longer exist in current pool; `execution/unmatched_closed_deals.json` is now empty, so current issue is stale historical buckets rather than active unmatched backlog.
- Logs show system activity continuing this morning (research-memory queries and fresh file writes), so not stalled, but there was a runtime error at `2026-04-24 00:54:07` where `job_live_monitor` hit `MT5 account_info() returned None`; nearby logs also emitted live-decay warnings for `core15_BTCUSDm_M15_df0a`, `core15_BTCUSDm_M15_f4f5`, `core15_BTCUSDm_M15_1a34`, and `core15_XAUUSDm_M15_8fd0`.
- Storage/cardinality note: `strategies/generated/` contains 4044 JSON files while only 117 are in current pool; safe to leave alone in daily cron, but any cleanup would need explicit approval because generated strategy deletion is destructive by current rules.
- No safe maintenance was performed because manifest/index were already fresh and the most obvious cleanup lever is destructive historical pruning, which daily cron should not do without Afu approval.
- 10:18 WIB: Prepared morning market brief for Afu. Public snapshot used: DXY around 98.8 and first weekly gain in 3 weeks, US10Y around 4.33 and firm for 5 straight sessions, gold around 4677 and under pressure below 4700, WTI around 96-97 after a sharp weekly surge, US equities mixed after Thursday dip but futures steadier on Intel-led earnings support, BTC hovering around 78k-79k with institutional flow backdrop positive but major resistance near 79k-80k. Core read: macro is risk-fragile rather than clean risk-on because oil shock + firmer yields support USD and cap duration/gold upside; BTC and equities still have selective upside if headlines calm, but headline risk from Hormuz/Iran remains the main intraday regime switch.
- autonomous_trading_ai: created `scripts/repair_legacy_pool_metadata.py` to backfill missing `strategy_explain.meta` fields in `strategies/pool_state.json`; correct invocation is `python -m autonomous_trading_ai.scripts.repair_legacy_pool_metadata` because direct script execution caused `ModuleNotFoundError` for package imports.
- Running the metadata repair refreshed pool artifacts safely: `Pool loaded/saved: 117 strategies`, `live_manifest.json entries=32`, `strategy_index.json entries=117`, and `Repaired 88 legacy records`.
- Afu explicitly asked to persist both the repair script and refreshed `strategies/pool_state.json` to git.
- Created `scripts/reconstruct_trade_regime_session_journal.py` to reconstruct trade-level journal from `execution/trades.log`, `execution/ticket_strategy_map.json`, live stats, and feature parquet files; output path is `execution/strategy_trade_journal.json`.
- First reconstruction run succeeded in writing the journal and reconstructing 99 trade rows across 53 live-stat strategy buckets, but entry/exit regime-session context remained empty because symbol names in logs looked like `XAUUSD`/`BTCUSD` while available feature parquet files used variants like `XAUUSDm`/`BTCUSDm`; next fix is symbol canonicalization/fallback mapping.
- XAU research work today: initial generator patch successfully changed family distribution so bullish families appeared in research population with `xau_impulse_pullback: 2` and `xau_session_continuation: 2`, confirming generator integration.
- Directed XAU research still failed after that first patch with `18 cheap_prescreen`, `1 semantic_duplicate`, and `1 weak_perf`; both bullish families had `0` accepted, shifting diagnosis from family weighting to bullish-XAU entry design quality.
- Real feature audit via `load_features('XAUUSDm', 'M15')` showed `fib_zone_382` was effectively absent in `trending_up`, so bullish XAU main paths should not depend on `fib_zone_382 == 1`.
- Durable design rule for bullish XAU templates: use real-feature-supported filters like `ma_short > ma_long`, `close > ma_short` or `close > ma_long`, moderate `trend_strength`, optional RSI/session filters; avoid over-constraining combinations like mandatory `fib_zone_382 == 1` plus narrow session and volatility gates.
- Bullish XAU template/parameter loosening was applied in `strategies/generator.py`, syntax check via `python -m py_compile autonomous_trading_ai\strategies\generator.py` passed, and rerun research was started.
- Git checkpoint committed successfully before further iteration: `0946ed5 Improve XAU research generation diagnostics`.
- Added new specialist family `xau_trending_up_specialist` in `strategies/generator.py` to explicitly target `trending_up`; it sets `playbook_type = xau_trending_up_specialist`, `allowed_regimes = ["trending_up"]`, `blocked_regimes = ["trending_down", "ranging"]`, and `preferred_sessions = ["london", "new_york"]`.
- Because generator requires both long and short rules, the specialist family uses an effectively disabled short side with `trend_strength < -0.99` so the family remains practically long-only while satisfying generator constraints.
- Propagated preferred regime metadata through explain path: `backtests/engine.py` now writes `preferred_allowed_regimes` and `preferred_blocked_regimes` into `trades_df.attrs`, and `backtests/explain.py` merges them into strategy `allowed_regimes` / `blocked_regimes`.
- Research with `xau_trending_up_specialist` confirmed plumbing was live, but latest meaningful family-stage result was still poor: `generated: 2`, `cheap_prescreen_fail: 2`, `accepted: 0`.
- Key XAU bullish diagnosis narrowed materially: earlier broader bullish templates produced trades but poor performance, while tighter specialist templates became too narrow and returned `0 trades`; next profitable path is a middle-ground specialist with medium coverage rather than broad long spam or ultra-tight session-only logic.
