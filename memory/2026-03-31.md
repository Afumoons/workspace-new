## 2026-03-31 — autonomous_trading_ai improvement notes

- Afu asked Clio to preserve two separate recommendation sets for improving autonomous_trading_ai strategy pass-rate / execution quality, to resume later after token reset.
- Context established:
  - `live_manifest.json` and `strategy_index.json` were being rebuilt successfully after scheduler rerun.
  - In the observed successful research cycle, `generated_at` changed but substantive contents mostly did not, because newly generated strategies were rejected (`low_trade_count` / `weak_perf`) and did not materially alter pool/live/index composition.

### Saved set A — Claude's recommendations (kept as Claude-origin suggestions)
1. Lower execution gate strictness:
   - `EXEC_MIN_TRADES: 200 -> 120`
   - `EXEC_MIN_WF_SHARPE: 0.75 -> 0.65`
   - `EXEC_MAX_DD_PCT: 12.0 -> 15.0`
   - `EXEC_MAX_CONSEC_LOSS: 15 -> 18`
2. Relax / remove hard reject on `avg_holding_bars < 1.0`, treating it as possible false positive due to same-bar entry/exit accounting.
3. Add bootstrap path for new strategies with low/zero `specialist_score`, allowing exploratory admission if other statistics are sufficiently good.
4. Increase `history_bars_default: 2000 -> 4000` to naturally increase trade counts and sample size.
5. Tune XAU generator parameter ranges, especially reducing overly high `trend_min` thresholds and biasing toward more executable ATR/threshold ranges.
6. Claude framed this as more aligned with prop-firm style, emphasizing realism for XAU M15 and concern that current gates may be too strict for specialist/range strategies.

### Saved set B — Clio's recommendations (kept as Clio-origin suggestions)
1. First priority: lower `EXEC_MIN_TRADES` because current threshold likely over-filters otherwise-valid specialists.
2. Tune XAU generator to reduce overly restrictive combinations and lower the frequency of 0-trade / very-low-trade candidates.
3. Add a bootstrap path for new strategies so exploratory candidates are not blocked purely by immature routing/specialist metadata.
4. Convert `avg_holding_bars` from a hard reject into a softer warning/penalty first, rather than removing the gate immediately.
5. Consider increasing history length (e.g. 3000–4000 bars), but monitor recency/staleness trade-offs.
6. Do NOT loosen all quality gates at once; change a few high-signal parameters first, then observe effect.
7. Prefer staged rollout order:
   - Stage 1: `EXEC_MIN_TRADES` reduction
   - Stage 2: bootstrap path for exploratory
   - Stage 3: XAU generator threshold tuning
   - Stage 4: soften `avg_holding_bars` filter
   - Stage 5: evaluate whether DD / WF Sharpe gates need relaxing
8. Clio's caution: Claude's direction is broadly good, but some claims (especially generalized prop-firm standards and assuming `avg_holding_bars` is definitely a bug) should be treated as hypotheses, not yet as proven facts.

### Saved set C — Claude–Clio synthesis after follow-up discussion
1. Shared high-value items:
   - add cheap pre-screen before full backtest / walk-forward / Monte Carlo,
   - strengthen family-memory / dead-zone penalization,
   - audit and improve execution realism, especially XAU spread/cost assumptions.
2. Claude's stronger follow-up points preserved:
   - `avg_holding_bars < 1.0` may be a false-positive hard gate due to same-bar entry/exit indexing,
   - specialist bootstrap problem is still real,
   - XAU cost realism may be materially under-modeled.
3. Agreed synthesis direction:
   - fix bugs / realism first,
   - improve candidate quality second,
   - relax gates selectively only after that.
4. Clio's caution on risk framing:
   - these are high-value / low-regret changes, but not literally zero-risk,
   - softening gates can admit noisier candidates,
   - improving cost realism can make the current pool look worse before it gets better.
5. Preferred phased order:
   - Phase 1: integrity / realism fixes (`avg_holding_bars`, cost model)
   - Phase 2: pipeline efficiency (cheap pre-screen, stronger dead-zone penalty)
   - Phase 3: candidate quality (explicit XAU playbooks, less over-conditioning, microstructure-aware ATR logic)
   - Phase 4: selective gate relaxation (`EXEC_MIN_TRADES`, history bars, possibly DD / WF Sharpe later)

### Resume cue for next session
When resuming, compare these three recommendation sets and decide which subset to implement first in `autonomous_trading_ai`, ideally with incremental patches and measurement after each change.
