## 2026-04-01 14:25 (Asia/Jakarta)
- Added size-based log rotation to `autonomous_trading_ai/logging_utils.py` using `RotatingFileHandler`.
  - `logs/system.log` now rotates at 5 MB per file.
  - Keeps 5 backup files to prevent unbounded growth.
- Updated `trading-bridge/bridge.py` symbol allowlist to remove `BTCUSDm`.
  - Reason: current HFM broker setup does not provide a `BTCUSD`/`BTCUSDm` market, so missing BTC updates are expected from broker symbol availability, not a generic feed bug.
- Validation: both edited Python files compiled successfully with `python -m compileall`.

## 2026-04-01 15:38 (Asia/Jakarta)
- Rotation patch hit Windows file-lock issue: `PermissionError [WinError 32]` during rollover of `autonomous_trading_ai/logs/system.log`.
- Hardened `autonomous_trading_ai/logging_utils.py` with a custom `SafeRotatingFileHandler`.
  - Uses `delay=True` to reduce eager file locking.
  - Falls back from rename-based rollover to copy+truncate when Windows denies rename because another handle/process still has the log file open.
- Validation: patched file compiled successfully.
- Git commit: `8f84fd8` — `Harden log rotation for Windows file locking`.

## 2026-04-01 15:45 (Asia/Jakarta)
- Safe-reset live execution state for a new real US-cent account.
- Reset files:
  - `execution/live_state.json`
  - `execution/equity_history.json`
  - `execution/closed_trades_state.json`
  - `execution/ticket_strategy_map.json`
  - `execution/open_trades.json`
- Preserved `execution/trades.log` (per Afu request: safe reset only).
- Baseline used for the new account state: equity/account start = `5880.0` (US cent account context).

## 2026-04-01 16:05 (Asia/Jakarta)
- Implemented Phase 1 account-switch / live-baseline guard in `autonomous_trading_ai/execution/live_monitor.py`.
- New behavior:
  - stores MT5 account identity (`login`, `server`, `currency`, `company`) alongside equity history,
  - detects account switch by login/server mismatch,
  - auto-refreshes baseline on account switch instead of treating it as real portfolio drawdown,
  - resets daily state and closed-deal bookkeeping during the baseline refresh,
  - enables a short warmup window (`warmup_cycles_remaining=3`) that skips the circuit breaker right after account switch.
- Added backlog note for future work in `tmp/account-switch-guard-backlog.md` covering Phase 2 (equity shock detection) and Phase 3 (disable reasons / audit trail / notifications).

## 2026-04-01 20:40 (Asia/Jakarta)
- Fixed family-metadata pipeline for live strategy persistence.
- Root cause: live manifest builder relied on `stats.strategy.family` / `playbook_type`, but persisted strategy payload was too thin and did not include `params`, so downstream family resolution often fell back to `unknown`.
- Changes made:
  - `scheduler/main.py`: persist richer `eval_result["strategy"]` including `params`, `stop_loss_pips`, and `take_profit_pips`.
  - `strategies/live_manifest.py`: stronger family fallback chain using `strategy`, `strategy.params`, and long/short family hints.
  - `strategies/pool.py`: family extraction helpers now also inspect nested `strategy.params` and long/short family hints.
- Added one-shot repair helper: `autonomous_trading_ai/tmp/repair_pool_family_metadata.py`.
- Repair run result: `repaired_records=0`, indicating older pool records had already lost family metadata upstream and will need refresh/repopulation rather than shallow patching.

## 2026-04-01 20:55 (Asia/Jakarta)
- Continued the previously postponed Phase 1 research-integrity work.
- Implemented in `scheduler/main.py`:
  - changed `avg_holding_bars < 1.0` from hard reject to warning-only,
  - added explicit XAU-specific backtest cost profile for research (`spread=0.60`, `commission_per_lot=7.0`, `slippage_pips=4.0`),
  - kept BTC-specific cost profile explicit,
  - added research logging of effective cost profile per symbol for auditability.
- Rationale:
  - reduce false negatives from same-bar / very-fast exits,
  - keep noise control via scoring and other gates instead of brittle hard rejection,
  - make XAU backtests more conservative and closer to live friction.
- Added note file: `autonomous_trading_ai/tmp/phase1-cost-holding-notes.md`.
