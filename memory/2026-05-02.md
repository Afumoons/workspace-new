## 2026-05-02
- User asked to investigate why the trading system had no trades; diagnosis found scheduler and engine were still running, with no-trade caused by gating logic rather than system failure: XAUUSDm blocked by regime gate plus no eligible specialists, BTCUSDm blocked by regime gate plus no active entry.
- Confirmed moving-average defaults used by the system are ma_short=20 and ma_long=50 in `research/features.py`, implemented as rolling simple moving averages rather than EMA.
- Manual trade workstream progressed from calculator-only UI into executable operator flow: manual-ticket now supports broker preview audit, explicit live submit, idempotent `client_submission_id`, and preserved `manual_user` segregation markers.
- Added real-time quote support for manual trading via UI/API integration so market-order entry price can track live MT5 bid/ask while keeping final broker fill subject to normal execution slippage.
- Successful commit recorded for live manual trade and realtime quotes: `2ea4183` — `Enable live manual trade submit with realtime quotes`.
