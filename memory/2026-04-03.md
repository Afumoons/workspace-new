## 2026-04-03 (Asia/Jakarta)
- Afu requested explicit naming for improvement streams because plain "Phase 1/2/3" references had become ambiguous.
- Created named improvement tracks in `autonomous_trading_ai/tmp/improvement-tracks.md`:
  - Track A — Research Pipeline Improvement
  - Track B — Live Monitoring & Attribution Improvement
  - Track C — XAU Exit Hardening Improvement
- Naming rule going forward: always refer to work as track + phase/subphase (e.g. `Track A / A2a`, `Track B / B2`, `Track C / C1`) instead of plain phase numbers.
- Afu prefers that when Clio creates/edits files in the workspace, Clio should commit those changes; if a task involves deleting files, Clio should tell Afu first and let Afu handle the commit for deletions.
- Track A was implemented in codebase practical terms:
  - A1: research integrity/realism completed (`avg_holding_bars < 1.0` warning-only, explicit XAU/BTC research cost realism)
  - A2: pipeline efficiency completed (cheap prescreen + stronger dead-zone/family-memory penalty)
  - A3: candidate quality completed (explicit XAU playbooks, less over-conditioning, microstructure-aware ATR logic)
  - A4: selective gate relaxation implemented selectively (`EXEC_MIN_TRADES` lowered selectively, specialist/exploratory thresholds added, `history_bars_default` raised to 4000, DD/WF Sharpe not broadly loosened)
- Decision rule agreed for `pool_state` after Track A:
  - do not reset immediately;
  - observe 3–5 research cycles first;
  - KEEP if pool remains informative and adapts;
  - PARTIAL RESET if stale/noisy candidates dominate but useful active/exploratory core remains;
  - FULL RESET only if old pool strongly pollutes evolution and partial cleanup is insufficient.
- Track B was implemented in practical terms:
  - B1: account-switch guard done
  - B2: strategy live stats reliability hardened via bootstrap path, fallback attribution using order/position_id/deal ticket, trades.log augmentation, and reconciliation helper
  - B3: audit/observability implemented with audit artifacts for unmatched closed deals and pool audit trail
- `strategy_live_stats.json` had stayed empty not because file creation failed, but because closed-deal attribution lineage was mismatching MT5 identifiers vs stored ticket map. This was mitigated by augmenting attribution from `execution/trades.log` and adding `scripts/reconcile_strategy_live_stats.py`.
- After reconciliation, `strategy_live_stats.json` was no longer empty and contained baseline strategy keys; note that this baseline may still show zero PnL/trade counts until future closed-deal attribution updates accumulate real stats.
- Track C was implemented in practical terms:
  - C1: stronger exit-governance penalties for exit-rule-heavy / weak exit signatures
  - C2: better XAU exit archetypes added (time-stop, ATR invalidation bias, session-guard style exits)
  - C3: practical advanced exit hardening implemented without native partial-TP engine support; native partial TP remains a possible future follow-up if needed
- Important nuance preserved: Track C is considered implemented practically, but native engine-level partial TP support is still not present and would need a separate future implementation if desired.
- Afu wants Clio to remember `C:\laragon\www\awesome-design-md` as a reusable design reference library for future website/web-app development, so Clio can consciously reference DESIGN.md-style inspirations when building frontend work.
- Afu agreed with a Hermes-inspired but OpenClaw-safe operational approach: Clio should improve workspace continuity through `tmp/` artifacts instead of changing OpenClaw core. Initial artifacts created:
  - `tmp/repo-maps/awesome-design-md.md`
  - `tmp/repo-maps/hermes-agent.md`
  - `tmp/playbooks/autonomous_trading_ai.md`
  - `tmp/playbooks/hermes-agent.md`
  - `tmp/decision-records/workspace-operational-legibility.md`
  - `tmp/validations/workspace-operational-legibility.md`
- Deferred-but-important idea saved for later implementation: a 4000-bar, one-open-position-aware min-trade threshold matrix. Recommended matrix to revisit in the future:
  - XAU: prescreen 20, research baseline 60, bootstrap 40, execution generic 120, execution specialist 100, execution exploratory 80
  - BTC: prescreen 20, research baseline 45, bootstrap 30, execution generic 100, execution specialist 80, execution exploratory 70
  - XAG: prescreen 20, research baseline 35, bootstrap 20, execution generic 80, execution specialist 60, execution exploratory 50
- Afu explicitly chose to defer implementing that full matrix for now, but wants it remembered because it may be implemented later.
