# 2026-04-04

- Clarified that current deep audit target repo is `autonomous_trading_ai` (not `autonomous_trading_ai2`).
- Reframed profitability concerns:
  - New York session blocking is intentional if research evidence supports it.
  - XAU high min-trades is intentional for stricter profitability targeting; may still deserve future tuning audit, but not treated as a bug.
  - Static spread assumptions are acceptable for now.
- Current priority selected by Afu: audit whether the active regime edge threshold is too tight.
- Deferred backlog captured in `tmp/playbooks/autonomous_trading_ai-profitability-backlog.md`:
  1. correlation/concentration control audit
  2. explicit guard for exploratory fallback on negative edge
  3. same-bar ambiguity instrumentation in backtests
  4. proactive live decay detection design
  5. Monte Carlo upgrade assessment
- Completed deeper correlation/concentration audit for `autonomous_trading_ai`:
  - `strategies/pool.py` has structural deduplication for exact logic clones.
  - Inactive pruning is family-aware, but live selection is not concentration-aware.
  - `strategies/live_manifest.py` simply ranks and takes top 16 per symbol/timeframe.
  - Current live manifest shows strong regime concentration:
    - BTC: 16/16 entries best_regime=`ranging`
    - XAU: 13/16 entries best_regime=`trending_down`, 3/16 `ranging`
  - Family metadata in current stored pool/manifest is mostly `unknown`, limiting immediate family-cap controls.
- Began patch for exploratory fallback sanity in `autonomous_trading_ai`:
  - updated `execution/signals.py` so keep-best exploratory fallback only survives when best regime edge is non-negative.
  - negative best-edge exploratory candidates now remain filtered out instead of being forced through fallback.
  - added a focused regression test around negative regime edge handling in `tests/test_routing_config.py`.
- Added same-bar ambiguity instrumentation to `autonomous_trading_ai/backtests/engine.py`:
  - engine now counts candles where both SL and TP are touched for an open position.
  - current fill policy remains pessimistic SL-first; instrumentation does not change trading outcomes yet.
  - new stats emitted: `same_bar_ambiguity_count`, `same_bar_ambiguity_stop_loss_count`, `same_bar_ambiguity_take_profit_count`, `same_bar_ambiguity_rate`.
  - added a focused backtest test in `tests/test_hardening_regime_and_generation.py` to verify ambiguity counting on a crafted candle.
- Began follow-up audit of ambiguity metrics in current `pool_state.json` / `live_manifest.json` to see whether same-bar ambiguity appears materially in stored strategy stats or whether historical pool entries need regeneration before the new metric becomes informative.
- New workflow preference from Afu: if local execution fails because a low-risk Python/module dependency is missing (example: `pytest`, `pyarrow`, `apscheduler`), Clio should install it directly and continue rather than asking for a second prompt.
# 2026-04-04

- Clarified that current deep audit target repo is `autonomous_trading_ai` (not `autonomous_trading_ai2`).
- Reframed profitability concerns:
  - New York session blocking is intentional if research evidence supports it.
  - XAU high min-trades is intentional for stricter profitability targeting; may still deserve future tuning audit, but not treated as a bug.
  - Static spread assumptions are acceptable for now.
- Current priority selected by Afu: audit whether the active regime edge threshold is too tight.
- Deferred backlog captured in `tmp/playbooks/autonomous_trading_ai-profitability-backlog.md`:
  1. correlation/concentration control audit
  2. explicit guard for exploratory fallback on negative edge
  3. same-bar ambiguity instrumentation in backtests
  4. proactive live decay detection design
  5. Monte Carlo upgrade assessment

- Result of active threshold audit on `autonomous_trading_ai`: current `active_regime_edge_threshold = 0.5` does not appear to be the live bottleneck in the present pool snapshot; active/exploratory records observed were already comfortably above the threshold.
- Result of deep audit on correlation/concentration control:
  - exact clone dedup exists in `strategies/pool.py`
  - inactive family-aware pruning exists
  - but live manifest selection is not diversity-aware, so live concentration control remains weak/non-explicit.
- Implemented in `autonomous_trading_ai` repo:
  - explicit runtime guard so exploratory fallback no longer keeps the best candidate if best regime edge is still negative
  - same-bar ambiguity instrumentation in backtest engine (diagnostic metrics only; fill model remains pessimistic SL-first)
  - added/updated tests, with focused pytest run passing after pytest was installed.
- Separate commits created:
  - workspace root commit for notes/backlog: `6c90056`
  - `autonomous_trading_ai` repo code commit: `462d869` (`Guard exploratory fallback and track same-bar ambiguity`)
- Important user workflow preference learned:
  - if a task fails because a common local Python dependency is missing (e.g. `pytest`, `pyarrow`), install it directly and continue without asking a second prompt, as long as it is a low-risk local dependency needed for the task.
- Installed during this session to unblock work:
  - `pytest`
  - `pyarrow`
- Result of sample-based ambiguity recompute audit:
  - reran representative active live strategies (8 XAU + 8 BTC)
  - observed `same_bar_ambiguity_count = 0` across the sample
  - current conclusion: same-bar ambiguity is instrumented and monitorable, but not a material priority based on the tested active sample.
- Current next major design direction: proactive live decay detection.
  - Existing system already has portfolio-level circuit breaker and per-strategy live realized PnL tracking.
  - Missing layer is per-strategy health/decay detection before account-level damage accumulates.
  - Recommended implementation approach: diagnostics + soft degrade first (warning/degrade), not immediate hard auto-disable.
  - Natural implementation path: create `execution/live_decay.py`, compute live-only decay signals from `strategy_live_stats`, log audit events, and integrate from scheduler after live stats update.
# 2026-04-04

- Implemented `autonomous_trading_ai` live hardening upgrades after earlier audit backlog:
  - proactive live decay detection v1 via `execution/live_decay.py`, integrated into `scheduler/main.py`, with focused tests in `tests/test_live_decay.py`
  - concentration control v1 in `strategies/live_manifest.py` (light caps with diversification-first selection when building live manifest)
  - concentration control v2 in `scheduler/main.py` for execution-stage diversified runtime pool selection
- Relevant repo commits created in `autonomous_trading_ai`:
  - `811343c` - `Add proactive live decay detection`
  - `7275abf` - `Add light concentration control to live manifest`
  - `23134ca` - `Diversify execution-stage strategy selection`
- Validation state reached during session:
  - live decay tests passed (`15 passed` at implementation step)
  - concentration-control-related test suite later validated green (`20 passed`) after installing missing local dependencies
- Additional low-risk local dependencies installed during this session to continue without extra approval prompts, following Afu's preference:
  - `apscheduler`
  - `chromadb`
- Important interpretation from config review/screenshot shared by Afu:
  - `max_portfolio_drawdown_pct = 30.0` is intentional, not a bug to "fix" back to 25
  - `daily_limits_enabled = False` and high `max_open_positions` confirm repo risk posture is intentionally aggressive
  - implication: upstream controls matter more than account-level hard stops, so priority of safeguards is confirmed as selection quality -> concentration control -> live decay detection -> realism upgrades
- User requested next housekeeping task after implementation wave:
  - update top-level documentation, module documentation, and instructions in `autonomous_trading_ai` so they align with the newest architecture/workflow changes
  - this docs alignment task was requested but not completed before compaction/flush
- New branch direction requested by Afu for `autonomous_trading_ai`:
  - create a dedicated sub-5m branch plan focused on `M5 -> M1 -> later tick-aware realism`
  - preserve specialist + disciplined routing philosophy
  - explicitly reject lottery-mode / near-certain-ruin design as the branch default
  - plan file created in repo docs as `docs/clio/08-SUB5M_BRANCH_PLAN.md`
