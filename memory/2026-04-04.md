# 2026-04-04

- Clarified that current deep audit target repo is `autonomous_trading_ai` (not `autonomous_trading_ai2`).
- Reframed profitability concerns:
  - New York session blocking is intentional if research evidence supports it.
  - XAU high min-trades is intentional for stricter profitability targeting; may still deserve future tuning audit, but not treated as a bug.
  - Static spread assumptions are acceptable for now.
- Current priority selected by Afu: audit whether the active regime edge threshold is too tight.
- Deferred backlog captured in `tmp/playbooks/autonomous_trading_ai-profitability-backlog.md`:
  1. correlation/concentration control audit
  2. explicit guard for exploratory fallback on negative edge
  3. same-bar ambiguity instrumentation in backtests
  4. proactive live decay detection design
  5. Monte Carlo upgrade assessment
- Completed deeper correlation/concentration audit for `autonomous_trading_ai`:
  - `strategies/pool.py` has structural deduplication for exact logic clones.
  - Inactive pruning is family-aware, but live selection is not concentration-aware.
  - `strategies/live_manifest.py` simply ranks and takes top 16 per symbol/timeframe.
  - Current live manifest shows strong regime concentration:
    - BTC: 16/16 entries best_regime=`ranging`
    - XAU: 13/16 entries best_regime=`trending_down`, 3/16 `ranging`
  - Family metadata in current stored pool/manifest is mostly `unknown`, limiting immediate family-cap controls.
- Began patch for exploratory fallback sanity in `autonomous_trading_ai`:
  - updated `execution/signals.py` so keep-best exploratory fallback only survives when best regime edge is non-negative.
  - negative best-edge exploratory candidates now remain filtered out instead of being forced through fallback.
  - added a focused regression test around negative regime edge handling in `tests/test_routing_config.py`.
- Added same-bar ambiguity instrumentation to `autonomous_trading_ai/backtests/engine.py`:
  - engine now counts candles where both SL and TP are touched for an open position.
  - current fill policy remains pessimistic SL-first; instrumentation does not change trading outcomes yet.
  - new stats emitted: `same_bar_ambiguity_count`, `same_bar_ambiguity_stop_loss_count`, `same_bar_ambiguity_take_profit_count`, `same_bar_ambiguity_rate`.
  - added a focused backtest test in `tests/test_hardening_regime_and_generation.py` to verify ambiguity counting on a crafted candle.
