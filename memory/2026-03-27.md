# 2026-03-27

- Afu requested a new operating rule: when given a very large/complex task that cannot be completed well in a single prompt, Clio should automatically use a cron-job approach by default.
- Implemented this as a workspace rule in `AGENTS.md` and created a reusable template in `tmp/cron-big-task-template.md`.
- Intent of the rule: staged execution, persisted checkpoints, milestone updates, and better continuity/reliability for oversized tasks.
- Added three specialized cron templates in `tmp/cron-task-templates.md`:
  - Riset Besar
  - Proyek Coding Besar
  - Monitoring / Automation Berulang
- Linked the general big-task template to the specialized template file so future large tasks can snap to the right operating pattern quickly.
