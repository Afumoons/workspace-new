# Decisions — 2026-03

Structured record of important decisions.

## Memory Architecture
- 2026-03-16 — Adopt `elite-longterm-memory` patterns:
  - Keep **Chroma** as the warm store (vector memory layer via `vector-memory` skill).
  - Introduce `SESSION-STATE.md` as hot RAM with write-ahead logging.
  - Keep `MEMORY.md` + `memory/` as curated archive and daily logs.
  - Avoid destructive commands from examples (e.g., `rm -rf` on memory directories) unless explicitly approved.
