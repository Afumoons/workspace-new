## 2026-05-01
- Monthly vector memory prune cron ran at ~10:02 WIB from `C:\Users\afusi\.openclaw\workspace` using `node scripts/pruneVectorMemory.mjs`.
- Script reached Chroma at `http://localhost:8000` but prune failed with `ChromaNotFoundError: The requested resource could not be found` while calling `getOrCreateCollection` for the target collection.
- Additional script/runtime warnings surfaced: chromadb client deprecation warning for `path` argument, and `Cannot instantiate a collection with the DefaultEmbeddingFunction. Please install @chroma-core/default-embed, or provide a different embedding function`.
- No retry loop was performed, per cron instruction.
- Daily `autonomous_trading_ai` governance run at ~19:40 WIB: overall status warning, not critical. Repo-visible state was fresh (`pool_state.json`, `strategy_index.json`, `live_manifest.json`, `system.log` updated around 13:33 UTC) and manifest remained consistent at 30 runtime entries (28 active, 2 exploratory).
- Main findings: repeated `No active/exploratory strategies in runtime set for XAGUSDm M15` persisted through the day; live decay warnings persisted for active BTC `rsi_range` names `core15_BTCUSDm_M15_e511`, `core15_BTCUSDm_M15_f4f5`, and `core15_BTCUSDm_M15_df0a`; latest observable account state stayed flat on the day (`daily_pnl 0`, `trades_today 1`) with one open BTC short from `core15_BTCUSDm_M15_88fa` showing small positive floating PnL.
- Pool composition remained concentrated and BTC-heavy: 31 active, 7 exploratory, 23 candidate, 56 disabled; family mix 77 `ma_trend`, 24 `rsi_range`, 15 `pullback_trend`, 1 `generic`; runtime manifest mix 17 `ma_trend`, 9 `rsi_range`, 3 `pullback_trend`, 1 `generic`; symbol mix 97 BTC vs 20 XAU, with no XAG entries in pool/index.
- Storage/cardinality remained notable but unchanged in risk posture: `strategies/generated/` grew to ~14,574 files (~18.8 MB) while indexed pool stayed at 117 entries. No destructive cleanup performed.
- No maintenance mutation executed because repo artifacts were already fresh/consistent and the cron forbids destructive cleanup without approval.
